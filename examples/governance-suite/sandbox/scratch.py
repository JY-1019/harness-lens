"""Sandbox zone — governed under the sandbox scope in **observe** mode: governance watches and
records, but does not block. Same risky action here is allowed (and recorded) instead of denied —
unless a session-level pin overrides the folder (see the precedence scenario)."""

x = 1 + 1
