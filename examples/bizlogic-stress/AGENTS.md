# bizlogic-stress — agent guide

This folder is governed by a deliberately HUGE harness (L1 governance + dozens of L2 business rules,
enforce). It exists to stress-test that a very long, complex 3-Layer harness loads, applies, shows in
the GUI, and still gates correctly (detector-backed L1 + DC-001) under a large number of tool calls.
Read a file before editing it.
